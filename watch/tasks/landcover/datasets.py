# Autogenerated via:
# python ~/code/watch/dev/maintain/mirror_package_geowatch.py
from geowatch.tasks.landcover.datasets import parser, print, print


def __getattr__(key):
    import geowatch.tasks.landcover.datasets as mirror
    return getattr(mirror, key)


def __dir__():
    import geowatch.tasks.landcover.datasets as mirror
    return dir(mirror)


if __name__ == '__main__':
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--coco_dset", type=str, required=True)
    args = parser.parse_args()

    coco_dset = WVDataset(args.coco_dset)
    print(len(coco_dset))

    img = coco_dset._load(coco_dset.gids[0])
    print(img.shape)
